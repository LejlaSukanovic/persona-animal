<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Persona Animal</title>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
      color: #212529;
      font-family: 'Arial', sans-serif;
    }
    .container {
      margin-top: 20px;
    }
    .card {
      background-color: #ffffff;
      border-radius: 15px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      position: relative;
      overflow: hidden;
      margin-bottom: 20px;
    }
    .card-title, .card-text, .attributes {
      text-align: center;
    }
    .card-title {
      margin-top: 20px;
      font-size: 1.5em;
      color: #17a2b8;
    }
    .attributes ul {
      list-style-type: none;
      padding: 0;
    }
    .attributes li {
      background-color: #e9ecef;
      margin: 5px 0;
      padding: 10px;
      border-radius: 5px;
    }
    .close-btn {
      position: absolute;
      top: 10px;
      right: 10px;
      color: #dc3545;
      font-size: 24px;
      cursor: pointer;
    }
    img {
      border-bottom: 5px solid lightblue;
    }
  </style>
</head>
<body>
  <div class="container text-center">
    <h1>Rezultat samoocenitve</h1>
    <br>
    <div class="card mx-auto" style="max-width: 500px;">
      <span class="close-btn">&times;</span>
      <img src="<%= entities[6].slika %>" class="card-img-top" alt="<%= entities[6].naziv %>">
      <div class="card-body">
        <p class="card-text"><h4>Vasa zival</h4> <%= entities[6].naziv %></p>
        <div class="attributes">
          <h4>Opis</h4>
          <p><%= entities[6].opis %></p>
        </div>
        <p class="card-text">
          <h4>Pozitivne lastnosti</h4>
          <%= entities[6].pozLastnosti %>
        </p>
        <p class="card-text">
            <h4>Negativne lastnosti</h4>
            <%= entities[6].negLastnosti %>
          </p>
      </div>
    </div>
  </div>

  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
  <script>
    document.querySelector('.close-btn').addEventListener('click', function() {
      this.closest('.card').style.display = 'none';
    });
  </script>
</body>
</html>
